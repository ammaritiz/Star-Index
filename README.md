# Returns Star-Index of a Developer

Suppose the star-index of a developer is s. Then the developer has s repositories, each with at least s stars whereas the remaining repositories has less than s stars.
